import { BiographyAssembler } from './biography.assembler';

describe('BiographyAssembler', () => {
  it('should create an instance', () => {
    expect(new BiographyAssembler()).toBeTruthy();
  });
});
